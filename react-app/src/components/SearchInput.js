import React from 'react';
import './SearchInput.css';

const SearchInput = () => {
  return (
    <input type="text" className="search-input" placeholder="Поиск монет..." />
  );
};

export default SearchInput;